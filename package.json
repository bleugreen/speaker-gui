{
  "name": "nodebootstraper",
  "version": "5.0.0",
  "dependencies": {
    "axios": "^0.21.1",
    "body-parser": "*",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "method-override": "*",
    "morgan": "*",
    "pug": "*",
    "redis": "^3.0.2",
    "serve-favicon": "*",
    "stylus": "*"
  },
  "engines": {
    "node": "*",
    "npm": "*"
  },
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
